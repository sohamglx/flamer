import flamer

@Get("/hello")
fn hello() -> String {
    return "Hello, world!"
}


@Flamer(port: 3000)
fn main() {

    flamer.get(
        '/users', 
        () {
            json(["alice", 
            "bob", 
            "charlie"])
        }
    )

    await flamer.listen()
}

await main()
